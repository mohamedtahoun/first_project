on:
  push:
    branches:
      - master




jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: "login to docker"
        run: docker login --username mbassem999 --password dckr_pat_j6pXeILicjrDWPch3vl1ylp35jk

